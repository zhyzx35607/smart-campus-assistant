.page {
  padding: 24rpx 32rpx;
  background: #f1f5f9;
  padding-bottom: calc(60rpx + env(safe-area-inset-bottom));
}

.card {
  background: #ffffff;
  border-radius: 32rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  overflow: hidden;
}

.card-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #0f172a;
  margin-bottom: 24rpx;
}

.switch-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 0;
  border-bottom: 1rpx solid #f8fafc;
}

.switch-item:last-of-type {
  border-bottom: none;
}

.switch-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex: 1;
  margin-right: 16rpx;
  min-width: 0;
}

.switch-icon {
  width: 72rpx;
  height: 72rpx;
  border-radius: 16rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 36rpx;
  flex-shrink: 0;
}

.switch-icon.blue { background: #dbeafe; }
.switch-icon.green { background: #dcfce7; }
.switch-icon.purple { background: #f3e8ff; }
.switch-icon.orange { background: #ffedd5; }
.switch-icon.pink { background: #fce7f3; }

.switch-info {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.switch-name {
  font-size: 28rpx;
  font-weight: 500;
  color: #1e293b;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.switch-desc {
  font-size: 22rpx;
  color: #94a3b8;
  margin-top: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-footer {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid #f1f5f9;
}

.footer-text {
  font-size: 22rpx;
  color: #94a3b8;
}

.action-row {
  display: flex;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.btn-cancel {
  background: #ffffff;
  border: 2rpx solid #e2e8f0;
  padding: 20rpx 32rpx;
  border-radius: 20rpx;
  font-size: 26rpx;
  color: #475569;
  font-weight: 500;
  white-space: nowrap;
}

.btn-reset {
  background: #ffffff;
  border: 2rpx solid #e2e8f0;
  padding: 20rpx 32rpx;
  border-radius: 20rpx;
  font-size: 26rpx;
  color: #475569;
  font-weight: 500;
  white-space: nowrap;
}

.btn-primary {
  flex: 1;
  background: linear-gradient(135deg, #2563eb, #4338ca);
  padding: 20rpx 32rpx;
  border-radius: 20rpx;
  font-size: 26rpx;
  color: #ffffff;
  font-weight: 700;
  text-align: center;
}

.notice-card {
  background: #eff6ff;
  border: 1rpx solid #bfdbfe;
  border-radius: 24rpx;
  padding: 24rpx;
}

.notice-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #1e40af;
  display: block;
  margin-bottom: 8rpx;
}

.notice-item {
  font-size: 24rpx;
  color: #1e40af;
  display: block;
  line-height: 1.8;
}
